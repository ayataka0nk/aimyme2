export type User = {
  id: string
  email: string
  name: string
}

export type ProjectSummary = {
  id: string
  name: string
  description: string
}

export type ProjectDetail = {
  id: string
  name: string
  description: string
}
